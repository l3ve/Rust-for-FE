{
  "name": "rust-addon",
  "version": "1.0.0",
  "description": "rust addon",
  "main": "main.js",
  "scripts": {
    "go": "electron .",
    "build": "cargo build --release && mv ./target/release/librust_addon.dylib ./index.node",
    "try": "npm run build && node main"
  },
  "keywords": [
    "rust",
    "addon"
  ],
  "author": "L3've",
  "license": "ISC",
  "devDependencies": {
    "electron": "^12.0.2"
  }
}